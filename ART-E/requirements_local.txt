# ===========================================
# ART-E Email Search Agent - LocalBackend 依存パッケージ
# ===========================================
#
# ローカル GPU でトレーニング・評価する場合はこのファイルだけでOKです。
#   uv pip install -r requirements_local.txt
#
# 注意: Python >= 3.11 が必要です。

# ART フレームワーク（backend extras: vLLM, Unsloth, TRL 等を含む）
# NOTE: openpipe-art 0.5.17 は gql<4 を要求するが、weave は gql>=4 が必要。
# art のコード自体は gql 4 で動作するため override で解決する (cf. OpenPipe/ART#644)。
openpipe-art[backend]==0.5.17

# LLM関連
openai>=1.0.0
litellm>=1.0.0

# LangChain (ツール変換用)
langchain-core>=0.1.0

# Weights & Biases (ログ・トレーニング)
weave>=0.50.0
wandb>=0.16.0

# データセット処理
datasets>=2.14.0

# ユーティリティ
pydantic>=2.0.0
tenacity>=8.0.0
tqdm>=4.65.0
python-dotenv>=1.0.0

# torchtune (LocalBackend のモデル管理)
torchtune>=0.6.0
